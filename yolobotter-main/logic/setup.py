from setuptools import setup, find_packages

setup(
    name='rzctl',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # Any dependencies your project needs (e.g., "requests")
    ],
)